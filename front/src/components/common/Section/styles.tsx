import styled from '@emotion/styled';

export const SectionContainer = styled.section`
  padding: 0 1rem;
  box-sizing: border-box;
  .section_content {
    padding-top: 3rem;
    padding-bottom: 3rem;
    max-width: 1024px;
    margin: 0 auto;
  }
`;
